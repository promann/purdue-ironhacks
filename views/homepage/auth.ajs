<h1 class="text-center">
    <% var uni = lien.user.profile.university; %>
    <% var uniName = uni.charAt(0).toUpperCase() + uni.slice(1); %>
    <% if (uni === "purdue") { %>
        GoldIronHack
    <% } else { %>
        BlackIronHack
    <% } %>
    <% var now = new Date() %>
    <%= now.getFullYear() %>
    — <%= uniName %>
</h1>
<% include("../posts") %>
